"use strict"

class Animal {
  constructor(legs,warmBlood,herbivore,layEggs) {
    this._numLegs = legs
    this._warmBlood = warmBlood
    this._herbivore = herbivore
    this._layEggs = layEggs
  }
}

module.exports = Animal
